// close all tags
// use camelcase for attributes, class -> className, for -> htmlFor
// using a single root element : each jsx expression must return single root element
const element=(
    <div class="container">
        <h1>Hello</h1>
        <img src="" alt="" />
    </div>
)